<script>import { get_current_component } from "svelte/internal";
import { El } from "../el";
export let componentName = "toast-container";
export let placement = void 0;
const components = [
  { component: get_current_component(), except: [] },
  ...$$props.components ?? []
];
$:
  cssProps = {
    placement
  };
</script>

<El {components} {...$$restProps} {componentName} {cssProps}>
	<slot />
</El>
