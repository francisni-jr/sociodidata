<script>import { get_current_component } from "svelte/internal";
import { El } from "../el";
export let componentName = "ribbon";
export let bgColor = void 0;
export let elementPosition = "end";
const components = [
  { component: get_current_component(), except: [] },
  ...$$props.components ?? []
];
let cssProps = {};
let otherProps = {};
$: {
  cssProps = {
    bgColor,
    elementPosition
  };
  otherProps = {
    componentName
  };
}
</script>

<El {components} {...$$restProps} {cssProps} {...otherProps}>
	<slot />
</El>
