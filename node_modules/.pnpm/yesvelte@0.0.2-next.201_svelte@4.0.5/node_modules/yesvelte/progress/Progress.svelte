<script>import { get_current_component } from "svelte/internal";
import { El } from "../el";
export let componentName = "progress";
export let size = void 0;
export let separated = void 0;
const components = [
  { component: get_current_component(), except: [] },
  ...$$props.components ?? []
];
let cssProps = {};
let props = {};
$: {
  cssProps = {
    size,
    separated
  };
  props = {
    componentName
  };
}
</script>

<El {components} {...$$restProps} {cssProps} {...props} role="progressbar">
	<slot />
</El>
