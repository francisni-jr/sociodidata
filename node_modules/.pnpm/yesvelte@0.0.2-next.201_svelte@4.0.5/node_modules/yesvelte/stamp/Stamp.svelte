<script>import { get_current_component } from "svelte/internal";
import { El } from "../el";
import { Icon } from "../icon";
export let componentName = "stamp";
export let bgColor = void 0;
export let textColor = void 0;
export let iconPosition = void 0;
export let size = "md";
export let icon = void 0;
const components = [
  { component: get_current_component(), except: [] },
  ...$$props.components ?? []
];
let cssProps = {};
let otherProps = {};
$: {
  cssProps = {
    size,
    iconPosition,
    bgColor,
    textColor
  };
  otherProps = {
    componentName,
    icon
  };
}
</script>

<El {components} {...$$restProps} {cssProps} {...otherProps}>
	<Icon name={icon} />
</El>
