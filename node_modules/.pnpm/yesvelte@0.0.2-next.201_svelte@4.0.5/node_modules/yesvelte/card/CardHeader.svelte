<script>import { get_current_component } from "svelte/internal";
import { El } from "../el";
export let componentName = "card-header";
export let light = void 0;
const components = [
  { component: get_current_component(), except: [] },
  ...$$props.components ?? []
];
let cssProps = {};
$:
  cssProps = { light };
</script>

<El {components} {...$$restProps} {cssProps} {componentName}>
	<slot />
</El>
