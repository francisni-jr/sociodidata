<script>import { get_current_component } from "svelte/internal";
import { El } from "../el";
export let componentName = "button-group";
export let size = void 0;
export let vertical = void 0;
const components = [
  { component: get_current_component(), except: [] },
  ...$$props.components ?? []
];
let cssProps = {};
let props = {};
$: {
  cssProps = {
    size,
    vertical
  };
  props = {
    componentName
  };
}
</script>

<El {components} role="group" {...$$restProps} {cssProps} {...props}>
	<slot />
</El>
