<script>import { El } from "../el";
import { get_current_component } from "svelte/internal";
export let tag = "tr";
export let componentName = "table-row";
export let color = void 0;
export let active = void 0;
const components = [
  { component: get_current_component(), except: [] },
  ...$$props.components ?? []
];
let cssProps = {};
$:
  cssProps = {
    color,
    active
  };
</script>

<El {components} {...$$restProps} {cssProps} {componentName} {tag}>
	<slot />
</El>
