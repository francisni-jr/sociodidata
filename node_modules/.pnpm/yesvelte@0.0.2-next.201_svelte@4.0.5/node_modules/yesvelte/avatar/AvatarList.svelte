<script>import { get_current_component } from "svelte/internal";
import { El } from "../el";
export let componentName = "avatar-list";
export let stacked = void 0;
const components = [
  { component: get_current_component(), except: [] },
  ...$$props.components ?? []
];
let cssProps = {};
let props = {};
$: {
  cssProps = {
    stacked
  };
  props = {
    componentName
  };
}
</script>

<El {components} {...$$restProps} {cssProps} {...props}>
	<slot />
</El>
